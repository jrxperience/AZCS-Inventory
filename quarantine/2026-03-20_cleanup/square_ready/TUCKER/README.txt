Use this folder only for Tucker.
TUCKER_ONLY_INVENTORY.csv = Tucker-only rows from the full Square upload file (417 rows).
TUCKER_IMAGE_QUEUE.csv = Tucker rows that already match the current Square export (161 rows).
TUCKER_IMAGE_PREP_QUEUE.csv = all Tucker rows with local images, ready for post-upload matching (406 rows).
TUCKER_IMAGE_MISSING.csv = Tucker rows still missing a local image (11 rows).
IMAGE_BATCHES/ = Tucker rows already matched to the current Square export.
IMAGE_PREP_BATCHES/ = all Tucker image-prep batches (2 batches).